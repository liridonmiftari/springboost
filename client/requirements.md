## Packages
framer-motion | Page animations and interactive feedback
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
The application generates a zip file on POST /api/generate.
The frontend must handle Blob responses for file downloads.
Dependencies are defined in @shared/routes.
